# UI 规范缺口记录

这些内容无法从 18 页原始 PDF 可靠取得。本阶段不设计完整样式，也不采用第三方组件库默认样式填补。

| 缺失内容 | 为什么后续需要 | 使用页面或组件 | 建议后续任务 | 需要确认 |
|---|---|---|---|---|
| 正式品牌、Logo 安全区/最小尺寸/反白规则 | 应用框架需要可靠品牌资产 | Navbar、登录或品牌入口 | 品牌确认后单独补充；阶段 3 仅保留配置能力 | 产品与设计 |
| 字体书面授权与跨平台像素级验证 | 当前字体由项目负责人提供并已接入，但仓库没有书面授权文件；不同系统回退渲染仍可能不同 | 全局排版、全部组件 | 阶段 4 前补齐授权记录，并在目标 Windows/macOS 环境复核 | 项目负责人、设计与资产负责人 |
| Loading、Spinner、Skeleton | 异步读取必须有反馈 | 全部业务页面 | 阶段 4 公共扩展组件 | 设计 |
| Toast、Notification、ResultFeedback | 提交、复制、资源操作需要结果反馈 | 购买、资源、存储、镜像、软件、网络 | 阶段 4 公共扩展组件 | 设计与产品 |
| Drawer | 较长轻量任务可能不适合小弹窗 | 上传、安装、网络规则 | 阶段 4 先定义，再由阶段 9 使用 | 设计 |
| 通用 Error Page、Empty State | PDF 只有空表格示例 | 全部列表、详情和异步区域 | 阶段 4 PageState | 设计与产品 |
| 响应式断点、最低宽度、移动/平板方案 | 1366 等视口需要稳定适配 | 应用框架、表格、表单、栅格 | 阶段 3 应用框架，阶段 4 组件补充 | 设计与工程 |
| 完整对比度规范与 Modal 焦点管理 | Task 04B 已补充 Focus Trap、焦点进入/返回和键盘关闭，但 PDF 没有完整对比度基线 | 后续公共组件、Modal | 保留无障碍回归测试，等待正式规范 | 设计与工程 |
| 弹窗遮罩、宽度、z-index、滚动、关闭策略 | Task 04B 已使用集中工程 Token 和可配置关闭策略，具体视觉仍非设计定稿 | Modal、提示弹窗 | 设计确认后替换 engineering Token | 设计与产品 |
| Tooltip 箭头及正式方位/触发时序 | Task 04A 已以工程 Token 实现视口定位和最小延时，但 PDF 未给定正式规则 | Tooltip、Sidebar、IconButton | 设计确认后替换工程暂定值 | 设计 |
| 表格精确行高、业务列宽、排序和固定列 | Task 04B 已实现通用行高、容器内横向保护与基础状态；业务列模型仍未知 | Table 与后续列表 | 业务页面阶段只增加已确认列，不扩展复杂数据平台 | 设计与产品 |
| Select 异步 Loading、无选项、Error、搜索 | Task 04A 只实现 PDF p.12 的通用本地选项选择；异步边界不能由基础组件臆造 | Select、购买与筛选表单 | 后续业务接入前由设计和产品确认 | 设计 |
| Tabs 正式溢出形式与路由同步 | Task 04B 已支持 Disabled 和工程横向滚动；路由不写死在组件内 | Tabs、后续详情 | 业务接入时由页面层同步路由 | 设计与工程 |

## Task 04B 保留缺口

- Grid 仍没有设计确认的响应式断点；1920 与 1366 只通过现有桌面内容容器自适应。
- Form 普通字段宽度、窄屏单列规则、底部操作是否固定仍未定义；当前使用集中工程 Token，操作区不固定。
- Table Loading/Error 使用克制的文字状态和公共重试按钮；后续统一 PageState 完成后可替换，不应由业务页面另画。
| 状态 Badge 与业务状态语义 | 资源、订单、操作记录需要紧凑状态 | StatusBadge | 阶段 4 只建视觉能力；业务枚举另行确认 | 产品与设计 |
| 监控图表、坐标轴、曲线和 Tooltip | CPU/内存/加速卡监控已确认 | MetricChart | 阶段 8 资源详情和监控 | 设计与监控负责人 |
| 动效时长与缓动 | Loading、浮层、页面切换需要一致行为 | 全局与公共组件 | 有明确动效规范后新增任务 | 设计 |

## 本阶段暂定的可访问性桥接

基础主题和 Task 04A 组件提供可见 `:focus-visible` 与 `prefers-reduced-motion`。由于 PDF 没有完整全局规则，当前只加入最小工程暂定 Token，并在命名中明确 `engineering`；后续 Modal、Tabs、Table 等组件继续补全状态矩阵。
