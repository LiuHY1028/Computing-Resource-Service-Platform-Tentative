# UI 规范冲突记录

本文件只记录 PDF 内部冲突及本阶段临时处理，不把临时处理升级为设计定稿。

| 编号 | 冲突页码 | 冲突内容 | 可能原因 | 当前临时处理 | 对后续实现的影响 | 需要设计确认 |
|---|---|---|---|---|---|---|
| UI-C01 | p.2、p.10 | 全局错误色与输入错误态色不同 | 组件专用校验色或规范版本差异 | 分别使用 `--color-error` 与 `--color-input-error` | Input 校验不能直接复用全局错误边框 | 是 |
| UI-C02 | p.2、p.8-p.12 | 通用边框与控件边框颜色不同 | 容器与控件场景分层 | 分别使用 `--color-border-default` 与 `--color-border-control` | 后续组件必须选对场景，不能统一成一个灰色 | 是 |
| UI-C03 | p.2、p.8 | 成功容器标注了错误语义的浅红背景 | 高概率为复制标注错误 | 成功容器暂用 p.2 成功背景与绿色边框 | 后续 Notice/Result 需等待确认 | 是 |
| UI-C04 | p.3、p.5、p.9、p.11-p.13、p.17-p.18 | p.3 主体系只有 330，组件页出现 300/305/380/480/Demibold | 变量字体连续字重与组件局部标注混用 | 保留全部数值；Demibold 按字体命名实例映射到 450，并保留 temporary 键 | 跨平台回退字体粗细可能不同；p.5 是否采用 450 仍待确认 | 是 |
| UI-C05 | p.3 | 字体样例为 MiSans VF，调用顺序从系统字体开始 | 资产说明与 CSS 调用说明未统一 | 负责人提供的 MiSans VF 优先；不可用时按系统回退链 | 仓库未包含书面授权文件，跨平台渲染仍需验证 | 是 |
| UI-C06 | p.8 | 多个无填充容器文字写作黑色填充，但画面为透明/白色 | 透明度漏标或标注模板错误 | 不实现黑色填充；按透明/白色场景处理 | 容器组件需保留无填充变体 | 是 |
| UI-C07 | p.2、p.9 | “警示按钮”和“危险按钮”都使用错误红色，功能警告色为橙色 | 按钮名称表示风险等级而非功能色阶 | 保留红色按钮结构；一般警告提示仍使用橙色 | 业务组件不得仅凭名称选择颜色 | 是 |
| UI-C08 | p.4 | Hover 区标注 40 px，但无法确认是宽度、高度或最小点击区 | 静态标注缺少指向说明 | 保留 `--layout-navbar-hover-area-temporary`，不用于正式 Navbar | 阶段 3 必须重新对照并确认交互热区 | 是 |
| UI-C09 | p.5 | 48 px 标注可能指固定区域、遮罩或菜单项相关高度 | 静态页面指向不清 | 不建立全局 48 px 菜单项高度 Token | 防止后续 Sidebar 被错误固定行高 | 是 |
| UI-C10 | p.11 | Radio/Checkbox 状态图可见约 16 px 控件，但页面没有可靠尺寸文字标注 | 图像示例未完整标尺化 | 使用 `--size-selection-control-engineering` 集中维护，不写成 PDF 已确认值 | 后续设计确认后只需调整一个组件语义 Token | 是 |

## 使用原则

1. 能区分组件或场景时保留独立语义 Token。
2. 不能区分时优先采用组件专用页的明确标注，同时保留 temporary 命名或文档说明。
3. 后续组件任务不得为减少 Token 数量而合并以上差异。

## Task 04B 核对结论

- p.17 Regular Table 表头字重 480 与 Compact Table 表头字重 300 属于明确的不同表格类型，不作为冲突合并；Compact 仅降低表头层级和行高，其余结构复用同一 Table。
- p.13 PromptModal 的 warning、danger 与 success 是不同语义场景；warning 继续使用 p.2 警告色，danger 使用错误色，没有套用 p.9 “警示按钮”名称对应的红色冲突规则。
- 本轮没有新增需要覆盖 UI-C01 至 UI-C10 的静默统一；已有组件专用 Token 继续优先。
